{* install database tables *}
<form n:name="tables" class="ajax" data-naja-history="off">

	{* form errors *}
	<ul n:snippet="errors" n:if="$form->hasErrors()" class="errors">
		<li n:foreach="$form->errors as $error">{_$error}</li>
	</ul>

	{* send button *}
	<input n:name="send" class="btn btn-primary text-white" data-btn-submit>

</form>
